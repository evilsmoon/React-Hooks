
export  const procesoPesado = ( interacciones ) =>{

        for (let i = 0; i < interacciones; i++) {
            // const element = interacciones[i];
            console.log(':v')
        }

        return `${interacciones} interacciones realizadas.`;
    }
